# Flat, npm-style node_modules layout, pinned so it never depends on nub's
# incumbent package-manager detection.
node-linker=hoisted
# The CLI framework is released alongside this package; take new versions at once.
minimumReleaseAgeExclude=@alleneubank/incur
